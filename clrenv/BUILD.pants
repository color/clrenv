python_sources(
    dependencies=[
        "//:environment",  # Include environment yaml files
        "//3rdparty:botocore",  # Not sure why this isn't picked up automatically.
        "//3rdparty/internal:clrypt",
    ],
)
# python_requirement(
#     name="clrenv",
#     requirements=["clrenv @ https://github.com/color/clrenv/archive/v0.2.0.tar.gz"],
# )
